

## Rà soát luồng phía sau (downstream) — đảm bảo logic thống nhất

Trước khi sửa luồng daily check, cần soi **toàn bộ luồng phía sau** để đảm bảo dữ liệu chảy đúng từ form submit → DB → tạo task → hiển thị cho ca sau / kho / bảo trì.
